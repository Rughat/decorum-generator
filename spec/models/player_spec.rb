require 'rails_helper'

RSpec.describe Player, type: :model do
  describe ".initialize" do
#     let(:scenario) { instance_double(Scenario) }
#     let(:goal_layout) { instance_double(House) }
#     let(:requirement) { instance_double(Requirement) }
#
#     it "creates a new player with requirements based on the goal house" do
#       allow(scenario).to receive(:goal_layout).and_return(goal_layout)
#       expect(Requirement).to receive(:new).with(goal_layout: goal_layout).and_return(requirement)
#
#       subject = described_class.new(scenario: scenario)
#
#       expect(subject.requirements).to all(be_a(Requirement)).and have_at_least(3).items
#     end
  end
end
